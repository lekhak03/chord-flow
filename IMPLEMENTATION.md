# V.0.1 Implementation Plan

## Overview
- Analyse FFT, extract chords, compares frequency with pre-defined mapping.
- Uses ML model somehow: Supervised Learning, L2 euclidean distance for each sliding window bucket, MSE Error but we render it to the user as well as a feedback (the tutoring side).

## Implementation
- [1] Use python to implement out the FFT extraction, create frequency mapping and either find existing wav file of particular mapping.
- [2] Match or fail for particular input - output pair.


## Progress So far:
- Currently Implementing [1]










## Future > V.0.1 Version Implementation
- C++ Backend Implemetaton for faster rendering on the UI.
-